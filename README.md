# Hdrihaven-Batch-Download-Category
A small script to download all HDRIs of a given category.

Currently used for generating scenes with lighting variation.

# Usage
 - `pip install -r requirements.txt`
 - `python run.py --help`
